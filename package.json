{
  "name": "signaling",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && xmit signali.ng"
  },
  "dependencies": {
    "@preact/signals": "^1.2.3",
    "preact": "^10.19.6",
    "sockette": "^2.0.6"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.2",
    "prettier": "^3.2.5",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
